services:
   server:
       build: server/
       command: python ./server.py
       ports:
           - 8080:8080
   client:
       build: client/
       command: python ./client.py
       network_mode: host
       depends_on:
            - server
